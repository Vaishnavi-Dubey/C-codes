// c program to add the two matrices by using multidimensional array.
// 03 addition of 2 matrices.c
#include <stdio.h>

int main() {
    int x, y, a[3][3], b[3][3], sum[3][3], i, j;
    printf("enter the number of rows: ");
    scanf("%d", &x);
    printf("enter the number of columns: ");
    scanf("%d", &y);

    printf("enter elements of first matrix: \n");
    for (i = 0; i < x; ++i)
        for (j = 0; j < y; ++j) {
            printf("enter element ")
        }

}



